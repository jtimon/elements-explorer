version: '2'

services:

  # dummydb:
  #   build:
  #     context: ../../
  #     dockerfile: docker/dummydb/Dockerfile
  #   ports:
  #     - "1984:1984"
  #   networks:
  #     main:
  #       aliases:
  #         - dummydb

  explorer:
    build:
      context: ../../
      args:
        - ENV_NAME
      dockerfile: docker/explorer/Dockerfile
    volumes:
      - /tmp/test-explorer-sql-postgres/target:/root/target
      - /tmp/test-explorer-sql-postgres/keys:/root/keys
    ports:
      - "5000:5000"
    networks:
      main:
        ipv4_address: 10.5.0.5
        aliases:
          - explorer
    depends_on:
      - bitcoin
      - elements
      - postgres

  elements:
    build:
      context: ../../
      args:
        - ENV_NAME
      dockerfile: docker/daemon/elements.docker
    volumes:
      - /tmp/test-explorer-sql-postgres/elementsregtest:/root/.elements
      - /tmp/test-explorer-sql-postgres/elementsparent:/root/.elementsparent
      - /tmp/test-explorer-sql-postgres/elementside:/root/.elementside
    networks:
      main:
        aliases:
          - elements

  bitcoin:
    build:
      context: ../../
      args:
        - ENV_NAME
      dockerfile: docker/daemon/bitcoin.docker
    volumes:
      - /tmp/test-explorer-sql-postgres/bitcoin:/root/.bitcoin
      - /tmp/test-explorer-sql-postgres/testnet3:/root/.testnet3
      - /tmp/test-explorer-sql-postgres/regtest:/root/.regtest
    networks:
      main:
        aliases:
          - bitcoin

  data:
    restart: "no"
    image: postgres:9.6
    volumes:
      - /tmp/test-explorer-sql-postgres/db/postgresql:/var/lib/postgresql/data
    command: "true"

  postgres:
    restart: "always"
    image: postgres:9.6
    volumes_from:
      - data
    ports:
      - "5432:5432"
    networks:
      main:
        aliases:
          - postgresalias

networks:
  main:
    driver: bridge
    ipam:
      config:
      - subnet: 10.5.0.0/16
