elementsregtest: ./$ELEMENTS_BRANCH_DIR/src/$ELEMENTS_DAEMON -printtoconsole -rpcbind=$ELEMENTS_HOST:$ELEMENTS_PORT -datadir=$ELEMENTS_DATADIR -rpcallowip=$ELEMENTS_ALLLOWFROM -rpcuser=$ELEMENTS_RPCUSER -rpcpassword=$ELEMENTS_RPCPASSWORD -zmqpubhashblock=$ELEMENTSREGTEST_ZMQ -logips -logtimestamps -server -txindex -listen -discover -validatepegin=0 -dbcache=8000 -maxmempool=8000 -regtest
