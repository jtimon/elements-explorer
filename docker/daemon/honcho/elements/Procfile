elementsregtest: ./$ELEMENTS_BRANCH_DIR/src/$ELEMENTS_DAEMON -rpcbind=$ELEMENTS_HOST:$ELEMENTS_PORT -datadir=$ELEMENTS_DATADIR -rpcallowip=$ELEMENTS_ALLLOWFROM -regtest -logips -logtimestamps -server -txindex -rpcuser=$ELEMENTS_RPCUSER -rpcpassword=$ELEMENTS_RPCPASSWORD -zmqpubhashblock=$ELEMENTSREGTEST_ZMQ -listen -discover -validatepegin=0 -persistmempool -dbcache=8000
